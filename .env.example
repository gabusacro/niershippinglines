# Supabase (get from Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# Anon key = "publishable" / public key (safe for client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Optional: Admin setup + trip/vessel deletes. Add for setup, then optionally keep for delete operations.
# Get from Supabase Dashboard > Project Settings > API > service_role.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# App URL (optional) — leave empty for local; set to production URL after deploy (e.g. https://your-app.vercel.app)
# Used for emails/links that need an absolute URL. Auth redirects use relative paths so work without this.
# NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# OpenWeather (Siargao) - optional for weather widget
NEXT_PUBLIC_OPENWEATHER_API_KEY=your-openweather-api-key

# WorldTides (Siargao tide) - optional; get key at https://www.worldtides.info. If not set, tide uses admin-entered times from dashboard.
# WORLD_TIDES_API_KEY=your-worldtides-api-key

# GCash deposit (optional) — shown on booking confirmation if set. Use for "Pay via GCash" instructions.
# NEXT_PUBLIC_GCASH_NUMBER=09XX XXX XXXX
# NEXT_PUBLIC_GCASH_ACCOUNT_NAME=Nier Shipping Lines

# Gmail SMTP (recommended, free) — sends booking/payment emails from your Gmail (e.g. gabu.sacro@gmail.com).
# Use an App Password: Google Account → Security → 2-Step Verification → App passwords. Do not use your normal password.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=gabu.sacro@gmail.com
# SMTP_PASS=your-16-char-app-password

# Resend (optional fallback) — used only if Gmail SMTP is not set.
# RESEND_API_KEY=re_xxxxxxxxxxxx
# RESEND_FROM=Nier Shipping Lines <onboarding@resend.dev>
